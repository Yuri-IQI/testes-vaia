[pytest]
# Registra os markers customizados para evitar warnings do pytest.
# Sem esse registro, rodar testes com @pytest.mark.integration
# gera um aviso de "unknown mark".
markers =
    integration: testes que carregam o modelo real de IA (lentos, requerem GPU ou CPU com memória suficiente)
